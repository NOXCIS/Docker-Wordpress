version: '2.1'

services:

  wordpress:
    image: wordpress
    restart: always
    ports:
      - 2095:80
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: exampleuser
      WORDPRESS_DB_PASSWORD: sm0b0uG9gfZTVKFQ
      WORDPRESS_DB_NAME: exampledb
    volumes:
      - ./wp-ftp:/var/www/html
    links:
      - db:db

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: exampledb
      MYSQL_USER: exampleuser
      MYSQL_PASSWORD: sm0b0uG9gfZTVKFQ
      MYSQL_RANDOM_ROOT_PASSWORD: '1'
    volumes:
      - db:/var/lib/mysql

volumes:
  wordpress:
  db:
networks:
  default:
    external:
      name: pihole_private_network
